h2 Edit Your Learning Model
.row
  .col-md-6.col-md-offset-3
    p Title:  #{learningModel.title}
    p Description: #{learningModel.description}
    p Uploaded: #{learningModel.createdAt}
    if flashMsg.length > 0
      .alert.alert-danger <strong></strong>#{flashMsg}
    form(method='POST' action="/review/"+learningModel._id)
      div.form-group
        label(for='title') Learning Model Title :
        input#name.form-control(type='text', value=learningModel.title name='title')
      div.form-group
        label(for='description') Learning Model Description:
        input#email.form-control(type='text', value=learningModel.description name='description')
      div.form-group
        input#name.form-control(type='hidden', name='_id' value=learningModel._id)
      button.btn.btn-primary(type='submit') Update Your Learning Model
    h2 Delete this Learning Model
    form(action="/review/delete/"+learningModel._id method='POST')
      input#delBtn(type="submit" name="delButton" value="Delete Learning Model")
